# 🧠 ДЕМО: Память контекста диалога

## ПРОБЛЕМА: БЕЗ ПАМЯТИ КОНТЕКСТА (ТЕКУЩЕЕ)

```
Диалог пользователя с ботом:

Пользователь: "Какие процедуры для закупок питания в школах?"
  ↓
Бот: detect_platform() = None (амбигуозно)
Бот: Спрашивает уточнение → "Вы имеете в виду omarket или goszakup?"
Пользователь: "На портале госзакупок"

Пользователь: "Какова минимальная стоимость?"
  ↓
Бот: detect_platform() = None (слово "стоимость" не в триггерах)
Бот: Снова спрашивает уточнение → "Вы имеете в виду omarket или goszakup?"
      (РАЗДРАЖЕННЫЙ ПОЛЬЗОВАТЕЛЬ ❌)

Пользователь: "Сколько дней должна длиться процедура?"
  ↓
Бот: detect_platform() = None
Бот: Снова спрашивает уточнение (3-й раз!) ❌❌❌
```

**ПРОБЛЕМА:** Каждый вопрос обрабатывается изолированно, без учета предыдущего контекста!

---

## РЕШЕНИЕ: С ПАМЯТЬЮ КОНТЕКСТА (НОВОЕ)

```
Диалог пользователя с ботом:

Пользователь: "Какие процедуры для закупок питания в школах?"
  ↓
Бот: detect_platform() = None
Бот: infer_topic() = "pitanie" (найдено слово "питани")
Бот: [ЗАПОМНИТЬ] Context: topic=pitanie
Бот: Возвращает информацию о закупках питания ✓

Пользователь: "Какова минимальная стоимость?"
  ↓
Бот: detect_platform() = None
Бот: infer_topic() = None (нет явных ключевых слов)
Бот: [ВСПОМНИТЬ] Context: topic=pitanie от предыдущего вопроса
Бот: RAG автоматически фильтрует по pitanie-документам
Бот: Возвращает стоимость питания (БЕЗ УТОЧНЕНИЯ!) ✓✓

Пользователь: "Сколько дней должна длиться процедура?"
  ↓
Бот: detect_platform() = None
Бот: infer_topic() = None
Бот: [ВСПОМНИТЬ] Context: topic=pitanie
Бот: RAG фильтрует по питанию
Бот: Возвращает сроки для питания (БЕЗ УТОЧНЕНИЯ!) ✓✓✓
```

**РЕЗУЛЬТАТ:** Естественный диалог без повторных уточнений! 🎉

---

## ДРУГОЙ ПРИМЕР: Переключение контекста

```
Пользователь: "Расскажи про закупки питания"
  ↓
Context: topic=pitanie

Пользователь: "Сколько там участников обычно?"
  ↓
Context REMEMBERED: topic=pitanie
Бот знает что вопрос о питании!

Пользователь: "А что если я работаю через omarket?"
  ↓
detect_platform() = "omarket" (явное упоминание)
Context UPDATED: platform=omarket, topic=pitanie (переключились!)
Бот теперь ищет информацию по omarket, но в контексте питания

Пользователь: "Как там делать покупки?"
  ↓
Context REMEMBERED: platform=omarket
Бот ищет в omarket-документах!
```

---

## АРХИТЕКТУРА

```
┌──────────────────────────────────────────────────────┐
│              CONVERSATION CONTEXT MANAGER             │
├──────────────────────────────────────────────────────┤
│                                                       │
│  User ID: 12345                                     │
│  Platform: goszakup (confidence: 0.95)              │
│  Topic: pitanie (confidence: 0.8)                   │
│  Last Updated: 2 minutes ago                        │
│  History: [Q1, Q2, Q3, Q4...]                       │
│                                                       │
│  Methods:                                           │
│  - update_context(question, platform, topic)       │
│  - get_assumed_platform()  ← Returns platform      │
│  - get_context_hint()      ← Sends to RAG search   │
│  - reset()                 ← New topic started      │
│                                                       │
└──────────────────────────────────────────────────────┘
                           ↓
                    [RAG SEARCH]
                           ↓
    Enhanced search query:
    "Какова стоимость?"
    [Context: platform=goszakup | topic=pitanie]
                           ↓
        Filters chunks by:
        - source_platform = "pitanie" OR
        - Has "питани" keywords
                           ↓
              Returns relevant answer ✓
```

---

## КЛЮЧЕВЫЕ КОМПОНЕНТЫ

### 1. **ConversationContext class**
```python
ctx = ConversationContext(user_id=12345)
ctx.platform = "goszakup"          # Сохраняет платформу
ctx.topic = "pitanie"              # Сохраняет тему
ctx.update_context(q, plat, topic) # Обновляет при новом вопросе
ctx.get_assumed_platform()         # Возвращает платформу из памяти
```

### 2. **infer_topic_from_question()**
Детектирует тему по ключевым словам:
- "питани" → "pitanie"
- "товаропроизводител" → "ktp"
- "внутристран" → "dvc"
- "магазин", "каталог" → "omarket"
- "закупк", "аукцион" → "goszakup"

### 3. **enhance_question_with_context()**
Добавляет контекст в вопрос для RAG:
```
Original: "Какова стоимость?"
Enhanced: "Какова стоимость?
[Context: platform=goszakup | topic=pitanie]"
```

---

## КОД ДЛЯ ИНТЕГРАЦИИ В BOT.PY

```python
from conversation_context import (
    ConversationContext, 
    infer_topic_from_question,
    enhance_question_with_context
)

async def handle_message(update: Update, context: ContextTypes.DEFAULT_TYPE):
    user_id = update.effective_user.id
    question = update.message.text.strip()
    
    # === НОВОЕ: Инициализируем контекст для пользователя ===
    if 'conversation_context' not in context.user_data:
        context.user_data['conversation_context'] = ConversationContext(user_id)
    
    conv_context = context.user_data['conversation_context']
    
    # === Детектируем платформу ===
    detected_platform = detect_platform(question)
    if not detected_platform:
        # Используем контекст если была платформа раньше
        detected_platform = conv_context.get_assumed_platform()
    
    # === Детектируем тему ===
    detected_topic = infer_topic_from_question(question)
    
    # === Обновляем контекст ===
    confidence = 0.9 if detected_platform else 0.6
    conv_context.update_context(question, detected_platform, detected_topic, confidence)
    
    # === Ищем с контекстом ===
    enhanced_question = enhance_question_with_context(question, conv_context)
    answer, chunks, _ = answer_question(enhanced_question, [])
    
    await update.message.reply_text(answer)
```

---

## ПРЕИМУЩЕСТВА

✅ **Естественный диалог** - Бот помнит о чем речь
✅ **Нет повторных уточнений** - Context запомнен
✅ **Умное переключение контекста** - Если пользователь переходит на другую тему
✅ **Лучший UX** - Диалог чувствует себя непрерывным
✅ **Работает с неоднозначными вопросами** - "Какова стоимость?" поймет в контексте питания

## РИСКИ

⚠️ **Старый контекст может помешать** - Если пользователь изменил тему, но забыл упомянуть
⚠️ **Memory bloat** - Если диалог очень длинный, история растет
⚠️ **Session expiration** - Контекст сбрасывается при перезагрузке бота
⚠️ **Требует сохранения состояния** - Нужно использовать context.user_data

---

## РЕКОМЕНДАЦИИ

1. **Сохранять контекст 5-10 минут** (уже в коде)
2. **Автоматически сбрасывать при явной смене темы** ("Новая тема", "Начать заново")
3. **Показывать текущий контекст пользователю** ("Я помню что вы про питание...")
4. **Позволять пользователю сбросить контекст** ("/reset", "/new_topic")
